# Uraniborg AI

Uraniborg AI website built with Hugo and the PaperMod theme.

## Local Development

```bash
hugo server -D
```

## Production Build

```bash
hugo --gc --minify
```

The site is deployed to GitHub Pages from the `main` branch. The custom domain is
managed through `static/CNAME`.
